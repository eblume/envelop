#!/bin/sh

# bootstrap debian on a LOCAL host
LOCAL_USER=${USER}

echo "Installing ansible..."
sudo apt install ansible

# NB: the initial version of this also installed sudo and set the proper
# sudo group, to avoid needing to enter a password. Instead, use --ask-become-pass
# to enter your user password. You can choose to set up no-password install if
# you wish.
