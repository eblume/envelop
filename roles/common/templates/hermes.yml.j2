# MANAGED BY ANSIBLE
# forked from ~/.tmuxinator/usual.yml

name: hermes
root: {{nonwork_code_dir}}/hermes

windows:
  - coding:
      layout: 'main-horizontal'
      panes:
        - poetry shell
        - poetry shell; and bin/hermes_tests.sh
