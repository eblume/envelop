# {{ ansible_managed }}
# forked from ~/.tmuxinator/hermes.yml

name: hermesweb
root: {{nonwork_code_dir}}/hermesweb

windows:
  - coding:
      layout: 'main-vertical'
      panes:
        - poetry shell
        - poetry shell
        - cd ./frontend; and poetry shell
        - poetry shell
