// {{ ansible_managed }}
// vim: set syntax=jsonc:
{
  "yaml.schemas": {
      "http://json.schemastore.org/kustomization": "kustomization.yaml",
      "kubernetes": "/*.yaml"
  }

  "yaml.format.enable": true,  // Enables yaml auto-formatting
  
  "diagnostic.displayByAle": true, // use ALE to show diagnostic info (disable if ALE is removed)

}
